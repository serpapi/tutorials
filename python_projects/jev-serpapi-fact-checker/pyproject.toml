[project]
name = "jev-serpapi-fact-checker"
version = "0.1.0"
description = "Check claims against SerpApi organic results with Jev through OpenRouter."
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32.5,<3",
    "serpapi>=1.1.1,<2",
]

[tool.uv]
package = false
